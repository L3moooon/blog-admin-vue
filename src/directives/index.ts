// 导入所有指令
import time from "./transformDate";
import btn from "./permission";
import overflow from "./textOverflow";
import loading from "./loading";

// 统一导出指令对象（key为指令名，value为指令配置）
export default {
  time,
  btn,
  overflow,
  loading,
};
